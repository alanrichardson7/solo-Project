import * as types from '../actions/actionTypes';

const initialState = {
    
};

const compositionsReducer = (state = initialState, action) => {
    switch(action.type) {
        case types.GET_COMPOSITION:

    }
}

export default compositionsReducer;